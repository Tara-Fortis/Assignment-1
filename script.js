// declare variables to grab DOM elements
const para = document.getElementsByClassName('story');

console.log(para);
// grab text from the DOM elements

// inject the selected element into p.story element